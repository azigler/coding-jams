const S=`
import type{DayConfig,p5}from'../types';const getSource=()=>"const S=\x60"+S+"\x60;"+S;const config:DayConfig={day:11,prompt:'Quine',creditName:'Manuel Larino',creditUrl:'https://mlarino.com/',setup:(p:p5)=>{p.createCanvas(800,800);p.textFont('monospace');},draw:(p:p5)=>{p.background(8,8,12);const src=getSource();const sz=9,lh=12;for(let i=0;i<src.length;i++){const c=src[i];if(c==='\x0a')continue;const row=src.slice(0,i).split('\x0a').length-1;const col=i-src.lastIndexOf('\x0a',i-1)-1;const x=4+col*sz*0.55;const y=12+row*lh;const h=(c.charCodeAt(0)*7)%360;p.fill('hsl('+h+',70%,65%)');p.text(c,x,y);}}};export const getDisplayedText=getSource;export default config;
`;
import type{DayConfig,p5}from'../types';const getSource=()=>"const S=\x60"+S+"\x60;"+S;const config:DayConfig={day:11,prompt:'Quine',creditName:'Manuel Larino',creditUrl:'https://mlarino.com/',setup:(p:p5)=>{p.createCanvas(800,800);p.textFont('monospace');},draw:(p:p5)=>{p.background(8,8,12);const src=getSource();const sz=9,lh=12;for(let i=0;i<src.length;i++){const c=src[i];if(c==='\x0a')continue;const row=src.slice(0,i).split('\x0a').length-1;const col=i-src.lastIndexOf('\x0a',i-1)-1;const x=4+col*sz*0.55;const y=12+row*lh;const h=(c.charCodeAt(0)*7)%360;p.fill('hsl('+h+',70%,65%)');p.text(c,x,y);}}};export const getDisplayedText=getSource;export default config;
