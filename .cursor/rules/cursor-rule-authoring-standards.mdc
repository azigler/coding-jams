---
description: Standards for authoring Cursor rules in the coding jams monorepo
globs: *.mdc
alwaysApply: false
---

# Cursor Rules Authoring Standards

## Description

This rule establishes standards for creating and maintaining MDC (Model-Driven Code) rules in the coding jams monorepo, ensuring consistency and clarity across all rules.

## Rule

1. Rule Location and Naming:
   - All rules MUST be stored in the `.cursor/rules` directory
   - Rule files MUST use the `.mdc` extension
   - Rule names MUST follow domain-based namespace scheme:
     - Format: `{domain}-{subdomain}-{descriptor}.mdc`
     - Example: `frontend-components-structure.mdc`
     - Example: `backend-api-endpoints.mdc`
     - Example: `database-schema-naming.mdc`

2. Each rule file MUST include a metadata header with:
   - `description`: Brief summary of the rule's purpose
   - `globs`: File patterns this rule applies to
   - `alwaysApply`: Boolean indicating if rule should be automatically applied
   - `domain`: Primary domain this rule belongs to (e.g., frontend, backend, database)

3. Each rule MUST contain these sections in order:
   - `Description`: Overview of what the rule does
   - `Rule`: Specific requirements and constraints
   - `Implementation`: How to apply and enforce the rule
   - `Benefits`: Why this rule exists and its advantages
   - `Examples`: Clear demonstrations of correct and incorrect usage

4. All sections MUST use proper markdown formatting

5. Examples MUST show both correct (✅) and incorrect (❌) cases

## Implementation

- The Cursor IDE will enforce this structure by:
  - Validating presence of required metadata fields
  - Checking for required sections in correct order
  - Ensuring proper markdown formatting
  - Verifying presence of both correct and incorrect examples
  - Validating domain-based namespace compliance
  - Confirming rule location in `.cursor/rules` directory

## Benefits

- Consistent rule documentation across the monorepo
- Clear understanding of rule purpose and implementation
- Easy validation of rule compliance
- Simplified maintenance and updates
- Better onboarding for new contributors
- Organized rule structure through domain-based namespacing
- Centralized rule location for easier discovery and management

## Examples

✅ Correct:

```markdown
---
description: Standardizes React component structure and organization
globs: src/components/**/*.tsx
alwaysApply: true
domain: frontend
---

# frontend-components-structure

## Description
Standardizes component file organization.

## Rule
[Clear rule requirements]

## Implementation
[Implementation details]

## Benefits
[List of benefits]

## Examples
[Both correct and incorrect examples]
```

❌ Incorrect:

```markdown
# Component Structure
---
description: Rules for components
---

Put components in src/components.
Use good names.
Make sure they work.
```
