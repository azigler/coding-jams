# Core dependencies
duckdb>=0.10.0
lancedb>=0.5.0
pandas>=2.0.0
pyarrow>=14.0.0

# Agent orchestration
langgraph>=1.0.0
dspy-ai>=3.0.0

# LLM providers (at least one required)
openai>=1.0.0
anthropic>=0.72.0

# PDF processing
pypdf>=6.0.0
pdfplumber>=0.10.0
PyMuPDF>=1.23.0

# Embeddings (choose one)
# Option 1: OpenAI embeddings (recommended)
# openai (already listed above)
# Option 2: Sentence transformers (alternative)
sentence-transformers>=2.0.0

# Data validation and utilities
pydantic>=2.0.0
python-dotenv>=1.0.0

# Optional but recommended
tqdm>=4.0.0  # Progress bars

